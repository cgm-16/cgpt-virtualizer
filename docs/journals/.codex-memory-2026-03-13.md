# Codex Memory - 2026-03-13

## User preferences

- Address the user as Ori.

## Current task

- Perform a full repository scan.
- Read existing GitHub issues.
- Open new GitHub issues for bugs, logical mismatches, and feature drift against `specs.md` and `whitepaper.md`.

## Starting state

- Starting branch: `main`
- Untracked at start: `.worktrees/`, `QA.md`

## Prior relevant findings

- Prior memory from 2026-03-10 records existing issues:
  - `#53` live ChatGPT selector mismatch causes immediate `Unavailable`
  - `#54` no recovery after initial selector miss or sub-threshold transcript state
  - `#55` per-tab enable state only lives in worker memory
  - `#56` fixture-heavy coverage misses live ChatGPT regressions
- Prior memory from 2026-03-09 records a historical packaging defect:
  - `dist/content.js` emitted as ESM for a classic MV3 content script
  - integration coverage masked that by loading content script as a module

## Audit notes

- Need to compare current implementation to strict v1 rules in `specs.md` and `whitepaper.md`.
- Need to read current GitHub issues before filing anything new to avoid duplicates.

## Existing issue review

- Open issues read:
  - `#50` test lint fix
  - `#53` live ChatGPT selector mismatch
  - `#54` no recovery after initial selector miss or sub-threshold startup
  - `#55` worker-memory tab state concern
  - `#56` fixture-heavy integration coverage gap
  - `#21` manual release checklist
- Important judgment:
  - `#53`, `#54`, and `#56` are still valid audit context.
  - `#55` is a product concern, but it does not conflict with the locked whitepaper because v1 explicitly keeps tab preference in service-worker memory keyed by `tabId`.

## New issues opened from this audit

- `#57` `버그: transcript bubble 수집이 top-level 계약을 깨고 nested match를 가상화한다`
- `#58` `버그: interaction pinning 미구현으로 selection/edit bubble이 detach될 수 있다`
- `#59` `버그: mid-session selector drift가 attribute/class 변경이면 감지되지 않는다`
- `#60` `버그: Unavailable popup이 토글을 막아 refresh 기반 복구 경로를 끊는다`

## Verification

- `pnpm test`
  - unit tests: passed (`21` files / `135` tests)
  - integration tests: failed in sandbox because Chromium aborted on launch
- `pnpm run test:integration` rerun with escalated permissions:
  - passed (`28` integration tests)
