# Codex 메모 - 2026-03-10

## 사용자 요청

- `QA.md`와 `specs.md`를 읽는다.
- 저장소 전체를 스캔한다.
- 확장이 동작하지 않는 원인 가설을 만든다.
- 가설과 수정 방향을 GitHub Issue로 연다.

## 시작 상태

- 시작 브랜치: `main`
- 시작 시점 untracked 항목: `.worktrees/`, `QA.md`

## 진행 메모

- 규칙에 따라 `origin/main`을 fetch + ff-only로 동기화했다.
- `git worktree add -b chore/extension-blocker-hypotheses .worktrees/extension-blocker-hypotheses main`로 작업용 worktree를 만들었다.
- worktree에는 `node_modules`가 없어서 `pnpm install --frozen-lockfile`를 실행했다.
- 확인한 명령:
  - `pnpm run build`
  - `pnpm run test:unit`
  - `pnpm run test:integration`
- 결과:
  - build 성공
  - unit test 21 files / 135 tests 통과
  - integration test 28 tests 통과

## 핵심 판단

- 현재 MV3 번들 산출물 자체는 로드 가능한 상태다.
- 실제 blocker 가능성이 가장 높은 지점은 live ChatGPT DOM 미호환, startup recovery 부재, worker 메모리 기반 상태 모델, synthetic DOM 위주의 검증 공백이다.

## 생성한 이슈

- #53 `버그: 실제 ChatGPT DOM selector 부재로 지원 경로가 즉시 Unavailable로 떨어진다`
- #54 `버그: 초기 selector miss 또는 50개 미만 transcript 상태가 같은 대화에서 회복되지 않는다`
- #55 `버그: 탭 활성화 상태가 워커 메모리에만 있어 매번 Off로 시작하고 쉽게 초기화된다`
- #56 `테스트: fixture 중심 검증만으로는 live ChatGPT DOM 회귀를 잡지 못한다`

## 추가 검토 메모

- Ori 요청: GitHub Issue #26, #28을 읽고 지금 시점에 고칠 가치가 있는지 판단한다.
- 로컬 기준점:
  - 오늘 확인된 더 직접적인 blocker는 #53~#56이다.
  - 현재 브랜치는 `main`이고 untracked 항목은 `.worktrees/`, `QA.md`다.
- 판단:
  - #26은 대부분 코드리뷰성 정리 항목이다. 현재 트리 기준으로 blocker는 아니고, 실사용 실패를 설명하지도 않는다.
  - #26에서 아직 남아 있는 실질 항목은 `isWorkerToPopupMessage` 단위 테스트 공백 정도다.
  - #28은 대부분 오래된 refactor 메모지만, 3번 bootstrap 타이밍 한계는 현재도 살아 있는 문제이며 오늘 연 #54와 사실상 같은 축이다.
  - #28의 5번 `handleContentMessage` 단위 테스트 누락은 이미 `tests/unit/content-controller.test.ts`로 해소됐다.
  - #28의 4번 `content.ts`의 `void` 지적은 현재 `bootstrapContentEntry(): Promise<void>` 구조로 바뀌어 더 이상 유효하지 않다.
