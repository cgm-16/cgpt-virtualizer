# Codex Memory - 2026-03-08

## User preferences

- Address the user as Ori.

## Current task

- Ori asked how to use the experimental Codex multi-agent workflow better.
- Relevant local context is the `spawn_agent` guidance in the active Codex tool instructions.

## Multi-agent notes

- Delegate only bounded sidecar tasks that do not block the next local step.
- Keep immediate critical-path work in the main conversation.
- Give each agent a concrete output, ownership, constraints, and acceptance check.
- Avoid duplicate delegation and avoid waiting unless blocked on the result.
- `CLAUDE.md` is directionally aligned on coordinator/worker structure and self-contained prompts.
- `CLAUDE.md` is not fully compatible with current Codex architecture because current worker agents can implement bounded code changes, not just read-only research.
- `CLAUDE.md` uses named specialist agents (`codebase-researcher`, `test-runner`, `lint-typecheck`, `figma-researcher`) that do not exist as first-class roles in this Codex session.

## Repo notes

- Task 4 in `todo.md` is implemented on `main` by merged PR `#33` (`feat/issue-7-transcript-scan`), including `src/content/transcript-scan.ts`, bootstrap integration, unit coverage, and Playwright coverage for 0/49/50 bubbles.
- In this sandbox, Playwright integration tests require escalated permissions to launch Chromium; unit tests run without escalation once dependencies are installed.
